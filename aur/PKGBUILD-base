# Maintainer: Joshua Li <josh@jrl.ninja>
# Please submit comments/issues here: https://github.com/JoshuaRLi/PKGBUILDs

# This PKGBUILD skeleton is tailored to the default way Github builds source dists for tagged releases.
# _ghrepo_ should be set to the repository name on GitHub. It may differ from the AUR pkgname.
# The git tags should follow vx.x.x. If this is not the case, change the url for source_x86_64.
# Modify as needed. This should suffice for a majority of simple packages.

pkgname=AUR_PKGNAME
pkgver=
pkgrel=1
pkgdesc=""
arch=('x86_64')
_ghrepo_=
url="https://github.com/JoshuaRLi/${_ghrepo_}"
license=('MIT')
#provides=()
#depends=()
#conflicts=()
#makedepends=()

source_x86_64=("${url}/archive/v${pkgver}.tar.gz")
sha512sums_x86_64=('')

_srcdir_="${_ghrepo_}-${pkgver}"

#build () {
#  cd "$_srcdir_"
#  # Build.
#}

#check () {
#  cd "$_srcdir_"
#  # Perform tests.
#}

package () {
  cd "$_srcdir_"
  # install -Dm 755 "path/to/built/EXAMPLE-BINARY" "${pkgdir}/usr/bin/EXAMPLE-BINARY"
  install -Dm 644 LICENSE "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
  install -Dm 644 README.md "${pkgdir}/usr/share/doc/${pkgname}/README.md"
}
